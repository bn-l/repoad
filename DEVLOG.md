- 2026-03-14: First Homebrew release (v0.1.0). No prior tags existed, so treated baseline as 0.0.0. Commit history contained a `feat:` commit → minor bump → 0.1.0 (already in Cargo.toml). Tagged `v0.1.0`, pushed, created formula in homebrew-tap using `clarg.rb` as a Rust CLI template, verified `brew install bn-l/tap/repoad` installs correctly and `repoad --version` reports 0.1.0.
- 2026-03-14: Added local directory mode. Running `repoad ./` was failing with "missing repo" because `parse_repo_path` expected `owner/repo` format. Fixed by detecting when the target is an existing directory on disk and skipping the clone step. Also replaced `walkdir` with the `ignore` crate so `.gitignore` rules are respected automatically. Output file is named after the directory (e.g. `repoad .` in a folder called `spabar` → `spabar.md`). Verified `target/` and other gitignored paths are skipped.
